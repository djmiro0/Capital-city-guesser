import { createContext } from "react"; //importing the createContext function

const AuthContext = createContext(null); //creating the context and assign it to AuthContext var.

export {AuthContext}; //export the AuthContext to the rest of the application.

